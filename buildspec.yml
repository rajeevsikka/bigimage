# aws codebuild configuration file
version: 0.1

environment_variables:
  plaintext:
    POWELL: "value"
            
phases:
  install:
    commands:
      - pip install -r ./cfm/requirements.txt
    
  build:
    commands:
      - echo $SHELL
      - env
      - ls -lR
      - set
      - python ./cfm/run.py -h > out.txt

artifacts:
  files:
    - out.txt
  discard-paths: yes
