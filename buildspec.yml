# aws codebuild configuration file
version: 0.1

environment_variables:
  plaintext:
    POWELL: "value"
            
phases:
  build:
    commands:
      - echo $SHELL
      - env
      - ls -lR
      - ./cfn/run.py -h > out.txt

artifacts:
  files:
    - out.txt
  discard-paths: yes
